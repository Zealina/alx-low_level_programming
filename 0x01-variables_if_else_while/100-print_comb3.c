#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int c;
	int d;
	int a;
	int b;
	{
		for (c = '0'; c <= '9'; c++)
		{
			return (a);
		}
		for (d = '9'; c >= '0'; c--)
		{
			return (b);
		}
	}
	putchar (a);
	return (0);
}
