#include <stdio.h>

/**
 * main - Print a string about proper grammar
 *
 * Return: print said string and end the program with zero
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
